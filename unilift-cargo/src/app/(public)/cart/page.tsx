import CartDetailsSection from '@/sections/cart';

export const metadata = {
  title: 'Your Cart | Safezy',
  description:
    'Review the items in your cart before proceeding to checkout on Safezy.'
};

const CartPage = () => {
  return <CartDetailsSection />;
};

export default CartPage;
